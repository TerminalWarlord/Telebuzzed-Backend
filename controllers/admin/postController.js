const Post = require('../../models/post');



const adminPostSubmission = async (req, res, next) => {
    const body = req.body;
    console.log(body);

    res.json({

    })
}

module.exports = {
    adminPostSubmission
}